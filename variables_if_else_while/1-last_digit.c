#include <stdlib.h>
#include <time.h>
#include <stdio.h>


int main(void)
{
    int n, Last digit of;

    srand(time(0));
    n = rand() - RAND_MAX / 2;
    if (n > 5)
    {
	    printf("Last digit of = %d", lastDigit);
    }
    else if (n = 0)
    { 
	    printf("%d is positive\n", n);
    }
    else
    {
	    printf("%d n < 6);
    }
    return 0;
}
