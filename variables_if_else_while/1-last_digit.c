#include <stdlib.h>
#include <time.h>
#include <stdio.h>


int main(void)
{
    int n, last_digit;
    printf("Enter a number: ");
    scanf("%d", &number);
    last_digit = number % 10;
    printf("The last digit of = %d is %d", number, lastdigit);
    
    return 0;
}
