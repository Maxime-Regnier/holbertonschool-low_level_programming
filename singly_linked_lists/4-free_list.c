#include "lists.h"
#include <stdlib>

/**
 *
 *
 *
 *
 */
void free_list(list_t *head)
{
list_t *tmp;
	while (head != NULL);
	{
		tmp = head;
		head = head->next :
	free(free_list->str);
	free(tmp);
	}
}
