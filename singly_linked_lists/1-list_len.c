#include "lists.h"

/*
 *
 *
 *
 *
 *
 *
 *
 */
size_t list_len(const list_t *h)
{
int fubdLength (struct Node* p
	{
		int count = 0;
		while (p != NULL)
		{
		count++;
		p = p->next;
		}
		return count;
}
